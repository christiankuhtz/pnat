#cloud-config
package_upgrade: true
write_files:
  - content: |
      Port SSHPORT
      TCPKeepAlive yes
    path: /etc/ssh/sshd_config
    append: true
runcmd:
  - systemctl restart ssh
power_state:
  mode: reboot
  delay: 1
  message: "cleanup reboot"
