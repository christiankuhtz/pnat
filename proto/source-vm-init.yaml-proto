#cloud-config
package_upgrade: true
packages:
write_files:
  - content: |
      TCPKeepAlive yes
    path: /etc/ssh/sshd_config
    append: true
runcmd:
  - [ systemctl, restart, ssh ]
power_state:
  mode: reboot
  delay: 1
  message: "cleanup reboot"
